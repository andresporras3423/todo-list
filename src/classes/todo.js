const Todo = (id, name, description) => (
  {
    id,
    name,
    description
  }
);

export default Todo;

// const todo = (id, title, description, dueDate, priority, projectId) => (
//     {
//       id,
//       title,
//       description,
//       dueDate,
//       priority,
//       projectId
//     }
//   );
  
//   export default todo;