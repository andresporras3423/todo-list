const Todo = (id, name, description, idProject) => (
  {
    id,
    name,
    description,
    idProject
  }
);

export default Todo;

// const todo = (id, title, description, dueDate, priority, projectId) => (
//     {
//       id,
//       title,
//       description,
//       dueDate,
//       priority,
//       projectId
//     }
//   );
  
//   export default todo;