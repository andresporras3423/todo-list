const todo = (id, title, description, dueDate, priority, projectId) => (
    {
      id,
      title,
      description,
      dueDate,
      priority,
      projectId
    }
  );
  
  export default todo;