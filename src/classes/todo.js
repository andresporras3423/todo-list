const Todo = (id, name, description, idProject, priority, duedate) => (
  {
    id,
    name,
    description,
    idProject,
    priority,
    duedate
  }
);

export default Todo;

// const todo = (id, title, description, dueDate, priority, projectId) => (
//     {
//       id,
//       title,
//       description,
//       dueDate,
//       priority,
//       projectId
//     }
//   );
  
//   export default todo;
